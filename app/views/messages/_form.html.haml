.col
  = form_for message, :remote => true do |form|
    = form.text_field :content, :id => "message_content_#{message.id}", :class => "formTextField"
    = form.submit "Send", :class => "formBtn"

= subscribe_to "/messages/new/#{message.id}"
